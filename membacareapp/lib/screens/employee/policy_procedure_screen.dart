import 'package:flutter/material.dart';

class PolicyProcedureScreen extends StatelessWidget {
  const PolicyProcedureScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
